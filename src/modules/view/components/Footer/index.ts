import renderModal from '../Modal';

const renderFooter = () => `
    <footer class="footer" id="footer"> 
      TODO написать футер
    </footer> 
    ${renderModal()}
  `;

export default renderFooter;
